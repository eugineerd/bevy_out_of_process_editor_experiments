[package]
name = "editor"
version = "0.1.0"
edition = "2024"

[dependencies]
editor_common = { path = "../editor_common" }
bevy = { workspace = true, features = ["dev", "bevy_remote", "bevy_feathers"] }
