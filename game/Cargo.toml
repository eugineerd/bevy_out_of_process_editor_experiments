[package]
name = "game"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = { workspace = true, features = ["bevy_feathers", "free_camera"] }
editor_integration = { path = "../editor_integration" }
